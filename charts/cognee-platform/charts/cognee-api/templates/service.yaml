apiVersion: v1
kind: Service
metadata:
  name: {{ include "cognee-api.fullname" . }}
  labels:
    {{- include "cognee-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cognee-api.selectorLabels" . | nindent 4 }}
