{
  "mcpServers": {
    "kubernetes": {
      "command": "npx",
      "args": ["-y", "kubernetes-mcp-server@0.0.59"],
      "env": {
        "KUBECONFIG": "${DEVPOD_KUBECONFIG:-~/.kube/devpod-${DEVPOD_WORKSPACE:-floe}.config}"
      }
    }
  }
}
